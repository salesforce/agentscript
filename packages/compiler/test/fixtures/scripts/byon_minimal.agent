# @dialect: agentforce

config:
    agent_name: "MinimalBYONBot"

start_agent Main:
    description: "Main entry point"
    reasoning:
        instructions: ->
            | Handle requests

subagent Shopper_Agent:
    description: "Minimal Commerce Cloud shopper agent"
    schema: "node://commerce/shopper_agent/v1"
